# The public URL of the backend API server.
# When deploying to Vercel, set this to the URL where your containerized backend is running (e.g., http://your-server-ip:5001).
NEXT_PUBLIC_BACKEND_URL=http://localhost:5001
